# ---- Site ----
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ---- Analytics (optional) ----
NEXT_PUBLIC_GA_MEASUREMENT_ID=

# ---- Advertising (optional; keep false until AdSense is ready) ----
NEXT_PUBLIC_ADSENSE_PUBLISHER_ID=
NEXT_PUBLIC_ADS_ENABLED=false
NEXT_PUBLIC_ADSENSE_SLOT_IDS=

# ---- Consent ----
NEXT_PUBLIC_CONSENT_MODE=basic

# ---- Contact email (server-side only — never prefix with NEXT_PUBLIC_) ----
CONTACT_EMAIL=
EMAIL_FROM=
EMAIL_API_KEY=
